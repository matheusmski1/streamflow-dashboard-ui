# Environment Configuration Example
# Copie este arquivo para .env.local e configure suas variáveis

# ===========================================
# API ENDPOINTS CONFIGURATION
# ===========================================

# REST API Base URL (Backend principal)
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Streaming/WebSocket URL (Para Server-Sent Events)
# Pode ser o mesmo backend ou um serviço separado
NEXT_PUBLIC_STREAM_URL=http://localhost:3001

# Alternative: Se você tiver endpoints separados
# NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1      # REST API
# NEXT_PUBLIC_STREAM_URL=http://localhost:3002          # Streaming Service

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================

# Application Info
NEXT_PUBLIC_APP_NAME=Streamflow Dashboard
NEXT_PUBLIC_APP_VERSION=1.0.0

# Environment Mode
NODE_ENV=development

# Development Mode (enables mock data when backend is unavailable)
NEXT_PUBLIC_DEV_MODE=true

# Debug Mode (enables detailed logging)
NEXT_PUBLIC_DEBUG=false

# ===========================================
# DEPLOYMENT CONFIGURATION (for production)
# ===========================================

# AWS Configuration (apenas para desenvolvimento local - NÃO usar em produção)
# Em produção, use IAM roles ao invés de chaves hardcoded
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1

# ===========================================
# EXAMPLES FOR DIFFERENT SCENARIOS
# ===========================================

# Scenario 1: Desenvolvimento local completo
# NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
# NEXT_PUBLIC_STREAM_URL=http://localhost:3001
# NEXT_PUBLIC_DEV_MODE=false

# Scenario 2: Frontend local + Backend na nuvem
# NEXT_PUBLIC_API_URL=https://api.exemplo.com/api/v1
# NEXT_PUBLIC_STREAM_URL=https://stream.exemplo.com
# NEXT_PUBLIC_DEV_MODE=false

# Scenario 3: Modo desenvolvimento (sem backend)
# NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
# NEXT_PUBLIC_STREAM_URL=http://localhost:3001
# NEXT_PUBLIC_DEV_MODE=true

# Scenario 4: Microserviços separados
# NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1      # Orders, Users, Auth
# NEXT_PUBLIC_STREAM_URL=http://localhost:3002          # Real-time Events
# NEXT_PUBLIC_DEV_MODE=false 